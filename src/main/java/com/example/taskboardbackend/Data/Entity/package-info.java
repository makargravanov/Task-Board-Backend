package com.example.taskboardbackend.Data.Entity;
/**
 * Тут находятся Entity - классы, являющие собой модели сущностей в БД и используемые JpaRepository для работы с ней.
 * Используйте Entity только на слое работы с базой (ServiceDB).
 * Скорее всего, менять эти классы пока не придётся. Так что, сначала спросите меня, если вам показалось, что это нужно.
 */